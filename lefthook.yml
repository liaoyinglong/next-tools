pre-commit:
  parallel: true
  commands:
    oxlint:
      glob: '**/*.{ts,tsx,js,jsx}'
      run: pnpm exec oxlint {staged_files}
    oxfmt:
      glob: '*'
      run: pnpm exec oxfmt --no-error-on-unmatched-pattern {staged_files}
      stage_fixed: true
