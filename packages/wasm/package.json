{
  "name": "@dune/wasm",
  "version": "1.0.0",
  "main": "./pkg/s_wasm.js",
  "types": "./pkg/s_wasm.d.ts",
  "files": [
    "pkg"
  ],
  "scripts": {
    "build": "cargo test && rm -rf ./pkg && wasm-pack build --target nodejs && vitest run",
    "test": "vitest",
    "test:u": "vitest -u"
  },
  "dependencies": {}
}
